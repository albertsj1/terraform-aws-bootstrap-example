# - id: pdm-lock-check
#   name: pdm-lock-check
#   description: run pdm lock --check to validate config
#   entry: pdm lock --check
#   language: python
#   language_version: python3
#   pass_filenames: false
#   files: ^pyproject.toml$
# - id: pdm-sync-all
#   name: pdm-sync-all
#   description: sync locked packages to pyproject.toml
#   entry: pdm sync --all
#   language: python
#   language_version: python3
#   pass_filenames: false
#   files: ^pdm.lock$
